{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "buildCommand": "npx vite build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}